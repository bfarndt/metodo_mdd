
CREATE TABLE daniel
(
	id bigserial,
	cpf text, endereco text,
	CONSTRAINT daniel_pkey PRIMARY KEY (id)
	
);

CREATE TABLE bruno
(
	id bigserial,
	cpf text, endereco text, nascimento date, mensalidade money, orientador bigint,
	CONSTRAINT bruno_pkey PRIMARY KEY (id)
	CONSTRAINT bruno_orientador_fkey FOREIGN KEY (orientador)
			REFERENCES $currentFeature/EntityType/entity/@name (id) MATCH SIMPLE
			ON UPDATE NO ACTION ON DELETE NO ACTION
	
);
