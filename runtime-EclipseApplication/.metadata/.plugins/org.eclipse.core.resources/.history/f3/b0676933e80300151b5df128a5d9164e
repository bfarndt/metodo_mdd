
CREATE TABLE daniel
(
	id bigserial,
	cpf text, endereco text,
	CONSTRAINT daniel_pkey PRIMARY KEY (id)
	
);

CREATE TABLE bruno
(
	id bigserial,
	cpf text, endereco text, nascimento date, mensalidade money, orientador bigint, teste bigint,
	CONSTRAINT bruno_pkey PRIMARY KEY (id)
	CONSTRAINT bruno_orientador_fkey FOREIGN KEY (orientador)
			REFERENCES daniel (id) MATCH SIMPLE
			ON UPDATE NO ACTION ON DELETE NO ACTION
	CONSTRAINT bruno_teste_fkey FOREIGN KEY (teste)
			REFERENCES daniel (id) MATCH SIMPLE
			ON UPDATE NO ACTION ON DELETE NO ACTION
	
);
